# Git
abbr "g"="git"
abbr "ga"="git add"
abbr "gaa"="git add --all"
abbr "gbd"="git branch --delete"
abbr "gcam"="git commit --all --message"
abbr "gcb"="git checkout -b"
abbr "gco"="git checkout"
abbr "gp"="git push"
abbr "gs"="git status"
abbr "gci"="git commit --allow-empty -m 'ci: trigger pipeline' && git push"
abbr "gcl"="git clone"


# Terraform/OpenTofu
abbr "terraform"="tofu"
abbr "tf"="tofu"
abbr "tfa"="tofu apply"
abbr "tfa\\!"="tofu apply -auto-approve"
abbr "tfap"="tofu apply -parallelism=1"
abbr "tfc"="tofu console"
abbr "tfd"="tofu destroy"
abbr "tfd\\!"="tofu destroy -auto-approve"
abbr "tfdp"="tofu destroy -parallelism=1"
abbr "tff"="tofu fmt"
abbr "tffr"="tofu fmt -recursive"
abbr "tfi"="tofu init"
abbr "tfir"="tofu init -reconfigure"
abbr "tfiu"="tofu init -upgrade"
abbr "tfiur"="tofu init -upgrade -reconfigure"
abbr "tfo"="tofu output"
abbr "tfp"="tofu plan"
abbr "tfs"="tofu state"
abbr "tfsh"="tofu show"
abbr "tft"="tofu test"
abbr "tfv"="tofu validate"
abbr "tfw"="tofu workspace"
abbr "tfwl"="tofu workspace list"
abbr "tfws"="tofu workspace select"

# tfbackend
abbr "tfb"="tfbackend"

# aws-sso-util
abbr "awsl"="aws-sso-util login"

# Kubernetes
abbr "k"="kubectl"
abbr "kg"="kubectl get"
abbr "kgp"="kubectl get pods"
abbr "kgd"="kubectl get deployments"
